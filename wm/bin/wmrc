# Environment variables for the whole desktop environment
# It's basically a wrapper around the currently used theme.
#
# (c) tudurom 2016 - wtfpl
#

test -z "$DISPLAY" && exit

. $HOME/bin/sourcecolors
DMENU_ARGS="-nb `xrq '*.background'` -nf `xrq '*.foreground'` -sb `xrq '*.foreground'` -sf `xrq '*.background'`"

# vim: set ft=sh :
